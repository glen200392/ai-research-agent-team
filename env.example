# ============================================================
# AI Research Agent Team — Environment Variables Template
# Rename this file to .env and fill in your values
# NEVER commit .env to git (already in .gitignore)
# ============================================================

# ------------------------------------------------------------
# LLM PROVIDER — configure one or more; system auto-detects
# DEFAULT_LLM_PROVIDER priority: anthropic > openai > google > ollama > lmstudio > openai_compatible
# ------------------------------------------------------------

# ── Cloud Providers ───────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-opus-4-6

GOOGLE_API_KEY=AIza...
GOOGLE_MODEL=gemini-2.0-flash

# ── Local Models: Ollama ──────────────────────────────────
# Install: curl -fsSL https://ollama.ai/install.sh | sh
# Pull:    ollama pull llama3.2
# Serve:   ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# ── Local Models: LM Studio ──────────────────────────────
# Install: https://lmstudio.ai  →  load a model  →  start local server
LMSTUDIO_BASE_URL=http://localhost:1234
LMSTUDIO_MODEL=local-model

# ── Custom OpenAI-Compatible Endpoint ─────────────────────
# Works with: vLLM, llama.cpp server, Jan, AnythingLLM, etc.
CUSTOM_API_BASE_URL=
CUSTOM_API_KEY=
CUSTOM_MODEL=

# Which provider to use by default (or leave as "auto" for detection)
DEFAULT_LLM_PROVIDER=auto              # auto | anthropic | openai | google | ollama | lmstudio | openai_compatible

# ------------------------------------------------------------
# SEARCH — at least one required for Intel Collector
# ------------------------------------------------------------
TAVILY_API_KEY=tvly-...                # Recommended
SERPER_API_KEY=...                     # Alternative: Google Search API
BRAVE_SEARCH_API_KEY=...               # Alternative: Brave Search

# ------------------------------------------------------------
# EMAIL DELIVERY
# ------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASSWORD=your-app-password        # Use App Password for Gmail
REPORT_EMAIL_RECIPIENT=your@email.com

# ------------------------------------------------------------
# OPTIONAL DELIVERY CHANNELS
# ------------------------------------------------------------
SLACK_BOT_TOKEN=xoxb-...
SLACK_CHANNEL=#ai-research

NOTION_API_KEY=secret_...
NOTION_DATABASE_ID=...

DELIVERY_WEBHOOK_URL=https://your-endpoint.com/webhook
DELIVERY_WEBHOOK_AUTH=Bearer your-token

# ------------------------------------------------------------
# PIPELINE SETTINGS
# ------------------------------------------------------------
REPORT_LANGUAGE=zh-TW                  # zh-TW | en | ja
QA_MIN_SCORE=85                        # 0-100, reports below this are revised
QA_MAX_REVISIONS=2
REPORT_OUTPUT_DIR=./reports/

# ------------------------------------------------------------
# FRAMEWORK-SPECIFIC (optional)
# ------------------------------------------------------------
# LangSmith tracing for LangGraph
LANGCHAIN_API_KEY=ls__...
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=ai-research-agent-team
